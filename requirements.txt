# FastAPI and Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0,<2.20
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# TTS Core
# Note: qwen-tts depends on onnxruntime which may need special installation
# Install qwen-tts separately after installing other dependencies
torch>=2.0.0,<2.6.0
torchaudio>=2.0.0,<2.6.0
transformers>=4.40.0,<4.60.0
accelerate>=0.25.0

# Audio Processing
soundfile>=0.12.1
librosa>=0.10.1
ffmpeg-python>=0.2.0
pydub>=0.25.1

# Model Download
huggingface-hub>=0.19.0
modelscope>=1.9.0

# Optional ASR (for voice sample transcription)
funasr>=1.0.0

# Utilities
numpy>=1.24.0,<2.0
aiofiles>=23.0.0

# Install these separately if needed:
# pip install qwen-tts --no-deps
# pip install onnxruntime or onnxruntime-gpu
